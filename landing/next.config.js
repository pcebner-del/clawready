/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standard Next.js — Vercel deploys this natively
  // Remove 'output: export' to support dynamic routes and API routes if needed later
}

module.exports = nextConfig
